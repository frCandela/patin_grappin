using System.Collections;
using System.Collections.Generic;

//using UnityEditor;
using UnityEngine;

//[CustomEditor(typeof(GrapplePoint)), CanEditMultipleObjects]
public class GrapplePointEditor// : Editor
{
   /* protected virtual void OnSceneGUI()
    {
        GrapplePoint example = (GrapplePoint)target;

        EditorGUI.BeginChangeCheck();
        Vector3 newTargetPosition = Handles.PositionHandle(example.targetPosition, Quaternion.identity);
        if (EditorGUI.EndChangeCheck())
        {
            Undo.RecordObject(example, "Change Look At Target Position");
            example.targetPosition = newTargetPosition;
        }
    }*/
}